server:
  port: 8083
spring:
  application:
    name: mall-canal
  cloud:
    nacos:
      config:
        file-extension: yaml
        server-addr: 127.0.0.1:8848
      discovery:
        server-addr: 127.0.0.1:8848
  main:
    allow-bean-definition-overriding: true
canal:
  server: 192.168.116.135:11111
  destination: example
#日志配置
#日志配置
logging:
  pattern:
    console: "%msg%n"
  level:
    root: error

elaticjob:
  zookeeper:
    server-lists: localhost:2181
    namespace: synctask

#动态定时任务案例
dynamiczk: localhost:2181
dynamicnamespace: dynamictask